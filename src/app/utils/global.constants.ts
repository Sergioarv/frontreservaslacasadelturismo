import { environment } from 'src/environments/environment';

export class GlobalConstants {

    public static urlEndPoint = environment.baseUrl;

    public static urlFlyer = '/flyer';
    public static urlFilterFlyer = '/filter';
}
